---
title: "Golang Gorm Cheatsheet!"
contentPreview: "Esta es una colección de snippets usando uno de los más famosos ORMs en golang: GORM.
Incluye cosas como"
createdAt: "2020-12-06"
---

Esta es una colección de snippets usando uno de los más famosos ORMs en golang: GORM.
```tsx
import React, { useState } from "react";
function Example() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>
        Click me
      </button>
    </div>
  );
}
```
